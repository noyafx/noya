{
  "scripts": {
    "setup": "git rm --cached .gitmodules && git add package-lock.json && git commit -m \"dependencies update\" && git push origin",
    "start": "node src/main.js"
  },
  "dependencies": {
    "discord.js": "^14.7.1",
    "mkdirp": "^2.1.4",
    "simple-json-db": "^2.0.0"
  }
}
